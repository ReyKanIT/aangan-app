# Dependencies
node_modules/
aangan_rn/node_modules/

# Expo
.expo/
aangan_rn/.expo/
aangan_rn/dist/

# Environment variables (contains secrets)
.env
aangan_rn/.env

# iOS / Android native builds
aangan_rn/ios/
aangan_rn/android/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Temp files
*.log
/tmp/
~$*

# Sensitive files
CREDENTIALS.md

# Claude
.claude/

# Web app
aangan_web/node_modules/
aangan_web/.next/
aangan_web/.env.local
aangan_web/tsconfig.tsbuildinfo
*.tsbuildinfo

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Root-level build artifacts (historical clutter — see archive/ for preserved versions)
/*.apk
/*.apk.idsig

# Release APKs (large binaries; downloaded on demand from EAS)
releases/
aangan_web/public/*.apk
aangan_web/public/*.apk.idsig

# Root-level brand assets & store graphics (keep in repo for reference, hide from git status)
/aangan_icon_*.png
/aangan_icon_*.svg
/aangan_logo.*
/play_store_*.png

# Old Flutter prototype (superseded by aangan_rn Expo + aangan_web Next.js)
aangan_flutter/

# Milestones docs
milestones/

# Stray root app.json (real one is aangan_rn/app.json)
/app.json

# Supabase local config (machine-specific)
supabase/.gitignore
supabase/config.toml

# Draft migrations not yet applied
supabase/migrations/create_app_settings.sql
